# Credenziali MySQL per docker-compose (valori DEV, non per produzione).
# Copia questo file in .env:   cp .env.example .env
# In produzione: valori forti da un secret manager; .env resta fuori dal repo (gia in .gitignore).
MYSQL_ROOT_PASSWORD=ghexp_dev_root
MYSQL_PASSWORD=ghexp_dev_pass
